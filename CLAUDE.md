# CLAUDE.md

## Project: OAS Buddy

An OpenAPI Specification (OAS) editor.

### Vision
- Edit OpenAPI Specs stored on disk locally, and (in a later phase) collaboratively on a server with colleagues.
- Support all versions of OAS (2.0/Swagger, 3.0, 3.1, ...) — MVP targets OAS 3.0 only; architecture designed to extend.
- Distribution: standalone desktop app first. An IntelliJ plugin is a likely later front-end reusing the same core engine.
- Programming language: Java (baseline: Java 25 LTS).

### MVP scope decisions
- UI: structured/form-based editor (no raw-text editing in MVP).
- OAS version: 3.0 only for MVP.
- Editable sections: Info, Servers, Paths/Operations, Components/Schemas — "everything at once" rather than a narrower slice.
- File formats: both YAML and JSON.
- Out of scope for MVP: OAS 3.1/2.0 support, server/collaboration backend, IntelliJ plugin, YAML comment preservation, external $ref resolution, raw-text/source view.

### Architecture
- Maven groupId and Java base package: `no.maddin.oasbuddy` (e.g. `no.maddin.oasbuddy.core.document`, `no.maddin.oasbuddy.desktop`). Artifact ids stay `oas-buddy` / `oas-buddy-core` / `oas-buddy-desktop`.
- Maven multi-module project.
  - `oas-buddy-core`: no UI deps. Document model, file I/O, validation. Reusable by future server/plugin front-ends.
  - `oas-buddy-desktop`: JavaFX app. Structured editor UI, depends only on oas-buddy-core's public API.
- Document model: Jackson-based ordered tree (ObjectNode/ArrayNode, LinkedHashMap-backed) as the single source of truth — preserves key order for git-friendly diffs. Loaded via jackson-dataformat-yaml / jackson-databind.
- Typed facade on top of the tree (OasDocument -> Info, Servers, PathItem, Operation, Schema, ...) for the UI to bind to. Reads/writes through to the same tree nodes (no separate DTO sync). MVP facade covers OAS 3.0 only.
- Validation: swagger-parser / swagger-core used only as a validation engine (spec-compliance errors/warnings), never as the editable model. This was a deliberate choice over using swagger-parser's own POJO model as the source of truth, because that model doesn't reliably preserve key order/round-trip fidelity and has only partial OAS 3.1 support — both matter for a git-collaborated editor.
- Also considered and rejected: KaiZen OpenAPI Parser (round-trip-preserving, editor-focused design, but unmaintained since 2019, OAS 3.0 only, EPL-1.0). openapi-processor/openapi-parser (actively maintained, supports 3.0/3.1/3.2, Apache 2.0, but not documented as round-trip/write-back oriented — a general parsing/validation library, not an editing model).

### Tech stack
- Language/runtime: Java 25 (LTS)
- Build tool: Maven
- UI framework: JavaFX, themed with [AtlantaFX](https://github.com/mkpaz/atlantafx) (Primer Light/Dark, switchable via View menu) instead of the default JavaFX/Modena look. Shared pane-building helpers live in `no.maddin.oasbuddy.desktop.pane.FormFields` (`root()`, `grid()`, `heading()`, `column()`, `columnHeading()`, `headerWithDelete()`) — every editor pane uses them for consistent spacing/typography rather than hand-rolling layout per pane.
- Theme choice (light/dark) persists across runs via `java.util.prefs.Preferences` (`MainApp.PREFS`, key `"theme"`) — plain JDK API, no extra dependency. If more settings accumulate later (window size, recent files), reconsider in favor of a JSON settings file for structured data; `Preferences` was chosen for now because theme is the only persisted setting.
- **Native access must be granted explicitly, and the grant differs per launch path.** JavaFX loads its native libraries through `System::load`, a restricted method: Java 24+ prints a four-line warning and a future release will block the call outright. So each way the app starts passes `--enable-native-access`, and *which* form is not interchangeable — the grant names a module only if JavaFX is on the module path. `javafx-maven-plugin` (`mvn javafx:run`) puts it there, so its `<options>` say `javafx.graphics`, and so does the jpackage bundle's `<javaOptions>` since it gained a module path (see Packaging below). Surefire (TestFX) still puts every jar on the classpath, so its `<argLine>` says `ALL-UNNAMED`. Naming a module the JVM doesn't know is itself a warning, so a single copy-pasted value for all three doesn't work.
- **Gotcha:** an XML comment cannot contain `--`, so a POM comment mentioning a JVM or jpackage flag by name breaks the POM with "in comment after two dashes (--) next character must be > not …". Spell the flag out in prose instead.
- OAS document tree: Jackson (jackson-databind, jackson-dataformat-yaml)
- OAS validation: swagger-parser v3 / swagger-core
- Testing: JUnit 5 (core), TestFX (desktop, minimal for MVP)
- License: Apache 2.0

### CI
- GitHub Actions (`.github/workflows/ci.yml`): builds and runs the full test suite (Java 25, `mvn test`) on push to `master` and on pull requests. The desktop module's TestFX tests need a display, so the job runs under Xvfb (`xvfb-run -a mvn -B test`).
- **Every workflow declares an explicit `permissions` block**, scoped to what it actually needs: `ci.yml` and `codeql.yml` read-only, `package.yml` `contents: write` (it attaches installers to a release), `release.yml` `contents: write` plus `actions: write` (it pushes a tag and then triggers `package.yml`). The repository default is already read-only, so this is defence in depth — a default is a setting that can change without the workflows noticing. CodeQL's `actions/missing-workflow-permissions` rule flags any workflow that omits it.
- GitHub Actions (`.github/workflows/package.yml`): builds native installers (.deb/.rpm/.pkg/.exe) via `jpackage` across an ubuntu/macos/windows matrix. Triggered manually or on a `v*` tag push, not on every push/PR — jpackage needs OS-native tools (dpkg-deb, rpmbuild, Xcode command line tools, WiX Toolset) that only exist on their respective OS, so this can't run as a single job. Installers are always uploaded as workflow-run artifacts; on a tag push they're additionally attached to the matching GitHub Release via `softprops/action-gh-release` (creating the release if it doesn't exist yet), using the version extracted from the tag name (see Versioning below).
- GitHub Actions (`.github/workflows/release.yml`): manual (`workflow_dispatch`, takes a `version` input) — creates and pushes an annotated `vX.Y.Z` tag, then explicitly triggers `package.yml` via `gh workflow run package.yml --ref vX.Y.Z`. No commits, no POM changes. The explicit trigger is required: a tag pushed with the default `GITHUB_TOKEN` does not fire other workflows' `push` triggers (GitHub's anti-recursion guard), so relying on `package.yml`'s `push: tags: ['v*']` trigger alone doesn't work when the tag came from a workflow. That trigger still fires normally for a tag pushed by a human with their own credentials outside Actions.

### Versioning
- **The release version lives in the `Release` workflow, not in the POMs.** The version numbers checked into `pom.xml` are placeholders and carry no meaning: `<revision>` (and `jpackage.appVersion`) are only defaults for ordinary local/CI builds, and every released artifact gets its version from the workflow input instead. Don't bump them to "prepare a release", and don't read them as "the current version" — `git tag` and the GitHub Releases page are the source of truth for what has shipped.
- CI-friendly versions: the POMs never hold a real version. Root `pom.xml` declares `<version>${revision}</version>` with a `-SNAPSHOT` placeholder as the default; both child modules' `<parent><version>` also reference `${revision}`. A release build overrides it with `-Drevision=X.Y.Z` — nothing in git ever needs a version-bump commit.
- Rejected: `maven-release-plugin` — it commits and pushes directly to the branch twice (version bump, next-`-SNAPSHOT` bump) and tags itself, which fights GitHub's branch-protection/PR-based workflow more than it helps, and was overkill for this project's needs.
- **The running app reads its own version from a Maven-filtered resource**, not the jar manifest and not a Java constant: `oas-buddy-desktop/src/main/resources/no/maddin/oasbuddy/desktop/version.properties` is filled in from the POM during the resources phase, `desktop.AppInfo` loads it and `pane.AboutDialog` shows it. That way `-Drevision=X.Y.Z` reaches the UI without a version-bump commit, and the name/url/license/copyright/developers it also carries exist in exactly one place (the POM) instead of being duplicated as constants.
- Filtering is scoped to that single file via two `<resources>` entries — one unfiltered that excludes it, one filtered that includes it. Declaring `<resources>` at all replaces the default, hence both; and the module ships the branding SVG, which the filter would be free to mangle if it were in scope. Nothing asserts the packaged SVG's bytes automatically; `mvn -Drevision=X.Y.Z package` followed by diffing the SVG out of the jar against `docs/branding/` is the check, done by hand when the resources config changes.
- A value Maven never substituted stays a literal `${...}`, and `AppInfo` treats that as absent: the version falls back to `"development build"` and every other field comes back empty, which the dialog renders by leaving that line out. This is what an IDE running straight from `target/classes`, or a bare test classpath, actually produces. It is also the mechanism that lets a fixed number of `developer.N.*` slots stand in for a list of any length — resource filtering has no loop construct, and Maven leaves an out-of-range index unsubstituted rather than failing the build.
- **Gotcha:** never use `${project.url}` in a child module. Maven appends the module's artifactId to an inherited `<url>`, so in `oas-buddy-desktop` it resolves to `https://github.com/maddingo/oas-buddy/oas-buddy-desktop`, which does not exist. The documented `child.project.url.inherit.append.path="false"` attribute does **not** suppress the append under Maven 3.9.16 — verified with a throwaway two-module project, not assumed. The root POM therefore holds a `repository.url` property, which `<url>`, `<organization>` and `version.properties` all read; properties inherit without rewriting.
- The author and license metadata the About dialog shows lives in the root POM (`<organization>`, `<inceptionYear>`, `<licenses>`, `<developers>`) because that is where Maven can hand it to the filter. `<developers>` credits people by name; the dialog also links to the GitHub contributor graph, which stays current with no POM upkeep. `<contributors>` was deliberately left out rather than maintained as a second, staler list.
- To cut a release: run the `Release` workflow with a version (e.g. `1.2.0`) → it tags `v1.2.0` → `package.yml` picks that up, builds installers with `-Drevision=1.2.0 -Djpackage.appVersion=1.2.0`, and attaches them to the GitHub Release for that tag.

### Packaging (jpackage)
- `oas-buddy-desktop/pom.xml` has three OS-gated Maven profiles (`jpackage-linux` → DEB + RPM + a portable APP_IMAGE, `jpackage-mac` → PKG, `jpackage-windows` → EXE), each producing a self-contained bundle (own JVM, no separate Java install needed on the target machine) via the `org.panteleyev:jpackage-maven-plugin`.
- The Linux APP_IMAGE isn't an installer — it's a plain directory (own JVM + native launcher script) meant for direct download/manual install, and as the fetch target for a future AUR `-bin` package (Arch/CachyOS doesn't understand `.deb`/`.rpm`; real AUR packaging is deferred). `package.yml` tars it into `oas-buddy-linux-x86_64.tar.gz` and deletes the raw directory before the artifact-upload/release-attach steps, since those glob `target/dist/*` and can't handle a raw directory as an asset.
- **JavaFX ships in its own subdirectory of the jpackage input and is launched from the module path**, because JavaFX loaded from the classpath logs `WARNING: Unsupported JavaFX configuration: classes were loaded from 'unnamed module @…'` at every start — an unsupported configuration, as the message says, not just noise. `maven-dependency-plugin` copies `org.openjfx` into `target/jpackage-input/javafx` and excludes it from the flat copy, and the launcher's java options name that directory as the module path and add `javafx.controls`.
  - jpackage puts **every** jar it finds under `--input` on `app.classpath`, nested ones included, so the JavaFX jars are on both paths; the named modules win and the classpath copies are shadowed. Removing them from the classpath isn't possible and isn't needed.
  - Each JavaFX artifact resolves to **two** jars — the main one, which holds nothing but a manifest, and the platform-classified one (`-linux`/`-mac`/`-win`), which holds the classes and the native libraries. Both claim the same module name, and with both in the directory the empty one can win: the app starts and then dies with `GraphicsPipeline.getPipeline()` returning null. An antrun `<delete>` removes the classifier-less jars, matched by the only thing that distinguishes them by name — their name ends at the version.
  - Surefire was deliberately left alone: its tests still run JavaFX from the classpath and still print that warning. Driving TestFX from a module path is fiddly, and a warning in test output is not worth the risk to the suite.
- Opt-in only: profiles activate on `-Djpackage` (combined with an OS check), so a plain `mvn package` is unaffected. Run `mvn -Djpackage package` on the target OS — the matching profile is picked automatically. Output lands in `oas-buddy-desktop/target/dist/`.
- Requires `dpkg-deb` (Linux/DEB), `rpmbuild` (Linux/RPM), Xcode command line tools (macOS/PKG), or WiX Toolset (Windows/EXE) to be installed locally — all preinstalled on GitHub's hosted runners except `rpmbuild` on Ubuntu, which the `package.yml` workflow installs explicitly.
- `jpackage --app-version` rejects a `-SNAPSHOT` suffix, so the desktop module tracks a separate `jpackage.appVersion` property instead of reusing `project.version`/`revision` directly. Its checked-in value is a placeholder like every other version in the POMs — a release build sets both it and `revision` explicitly from the workflow input (see Versioning above).
- The one case where a placeholder version still bites: macOS PKG rejects a major version of `0`, so a local `mvn -Djpackage package` on a Mac fails unless `-Djpackage.appVersion=` is passed with a `1.x`-or-later value. CI releases always pass it explicitly and are unaffected.

### A window that never appears (Wayland/Hyprland)
- `tools/diagnose-window.sh` answers "the app is running but I can see no window" — reads only, prints session, process, X11 view, compositor view, an all-windows comparison table, and the options that hide a new window.
- **The usual cause is not in the app:** a tiling compositor stacks a newly opened window *behind* an existing fullscreen window on the same workspace and nothing brings it forward (Hyprland's `misc:on_focus_under_fullscreen` and `misc:focus_on_activate` both default to 0). `hyprctl dispatch focuswindow class:no.maddin.oasbuddy.desktop.MainApp` retrieves it.
- **A lone `visible: 0` proves nothing — the comparison table is the actual diagnostic.** When sibling *native-Wayland* windows on the same workspace also report `visible: 0` and `acceptsInput: 0`, those flags are the compositor reporting occlusion, not something JavaFX failed to do. Read without the siblings they look exactly like an app-side focus bug, which is the wrong trail.
- **X11 is the wrong authority under Wayland.** `xwininfo` calls the hidden window `Map State: IsViewable`, and the XWayland coordinates it reports are not the compositor's real ones. Ask `hyprctl`; only fall back to `xprop` when there is no compositor to ask.
- Hyprland renamed the knob — `misc:new_window_takes_over_fullscreen` does not exist as of 0.56.2, it is now `misc:on_focus_under_fullscreen`. Current names are listed in `~/.config/hypr/hl.meta.lua`; don't guess them.
- **Setting that knob to 1 is not the free fix it looks like**, so don't recommend it blind: it also lets any short-lived toplevel that takes focus — browser tooltips, splash windows — seize the fullscreen state and carry it off when it closes. A keybind bound to the `focuswindow` dispatch fixes the OAS Buddy case with no such blast radius.

### Branding / app icon
- `docs/branding/oas-buddy-icon.svg` is the **single hand-edited source** for the icon (a `{` brace beside an outline tree — spec + structured editor; deliberately not derived from the OpenAPI Initiative's trademarked logo). Palette: badge `#0F243F`, mark `#2DD4BF`, tree `#7DD3FC`. Everything else is generated by `tools/generate-icons.sh` and committed, so CI never needs image tooling. See `docs/branding/README.md`.
- The in-app icon is **rendered as live vector, not loaded from a bitmap**: `desktop.AppIcon` parses that SVG with the JDK's built-in DOM parser (no new dependency) into a JavaFX scene graph and `snapshot()`s it at each size `MainApp` asks for.
- **Gotcha, learned the hard way:** `AppIcon.image()` re-encodes each snapshot to PNG and decodes it again before returning. This looks redundant but is load-bearing — the GTK glass backend does *not* forward a `snapshot()`-produced `WritableImage` to the window manager, so putting raw snapshots in `Stage.getIcons()` leaves X11's `_NET_WM_ICON` unset and the window/taskbar silently keeps the default Java icon, even though `getIcons()` looks perfectly populated. A decoded image propagates; a snapshot doesn't. Confirmed by running two otherwise-identical stages side by side and comparing `xprop -id <win> _NET_WM_ICON`. `AppIconRenderTest.imagesAreDecodedNotRawSnapshots` guards it, since no pixel assertion can catch this. To check by hand: `xprop -root _NET_CLIENT_LIST`, then `xprop -id <id> _NET_WM_ICON`. JavaFX's `Image` can't read SVG, and the obvious alternative — copying the path data into Java constants — would have created a second copy of the geometry that silently drifts from the first. The parser deliberately handles only `rect`/`path`/`circle`/`g`; `AppIconResourceTest` asserts the SVG stays inside that subset, since anything else is ignored rather than reported.
- Rasters exist **only** because `jpackage --icon` rejects SVG and needs a different format per OS (PNG/ICO/ICNS). The format is selected by one `jpackage.icon` property that each `jpackage-*` profile overrides, referenced once in the shared `pluginManagement` config.
- **On Linux the app name jpackage is given is the lowercase `oas-buddy`**, overridden in the `jpackage-linux` profile only, so the installed command is `/opt/oas-buddy/bin/oas-buddy` and the app-image directory (and therefore the tarball's top-level entry) is `oas-buddy` — not a path with a capital and a space in it. macOS and Windows keep `OAS Buddy`, which is what their bundles display. jpackage looks the desktop-entry resource up **by app name**, so `src/main/jpackage/linux/oas-buddy.desktop` has to be renamed in step with it, and its `Name=` is spelled out rather than left as jpackage's placeholder — otherwise the application menu would read "oas-buddy".
  - **Gotcha:** jpackage substitutes its placeholders everywhere in that file, *comments included*. A comment that names one gets rewritten in the shipped file.
  - `StartupWMClass=no.maddin.oasbuddy.desktop.MainApp` is what lets the shell match the running window to the entry, so a taskbar/dock shows the app icon and "OAS Buddy" rather than an unknown window. **The value is a duplicate of a Java class name**, and renaming or moving `MainApp` would break the association silently — it still compiles, packages and starts — so `LinuxDesktopEntryTest` asserts the entry's `StartupWMClass` equals `MainApp.class.getName()`. It reads the file out of the source tree, since packaging input is not on the classpath. Read the live value with `xprop -root _NET_CLIENT_LIST` then `xprop -id <id> WM_CLASS` — here both the instance and class fields hold the FQCN.
  - `Categories=` is spelled out too. jpackage fills it from its menu-group option, which is a different thing from the app category the POM sets (that one becomes the DEB `Section`/RPM `Group`), and it defaults to the literal `Unknown` — which is what shipped up to and including v1.2.0.
- **Nothing puts the launcher on `PATH`, deliberately.** jpackage has no option for it, so a package-owned symlink would mean vendoring its Debian `postinst`/`prerm` and the RPM `.spec` — the same cost that was rejected for the icon, for a desktop app whose normal entry point is the menu entry. The README tells the user to link `/opt/oas-buddy/bin/oas-buddy` into `/usr/local/bin` instead, which is the FHS location for exactly that and the only route that also covers the portable app-image. Note that a *package* may not write there (Debian policy); `/usr/bin` would be the target if this is ever revisited, and an AUR `PKGBUILD` is where Arch would do it.
- Linux gets a genuinely scalable icon: the SVG is copied into the jpackage input (so it ships inside the package) and `src/main/jpackage/linux/oas-buddy.desktop` — supplied via `--resource-dir` — points `Icon=` at its absolute installed path. An absolute path was chosen over installing into the hicolor theme under a theme name, because a theme name would require vendoring jpackage's `postinst`/`postrm` *and* its RPM `.spec` template just to copy one file; the absolute path covers DEB and RPM identically with nothing to keep in sync against the JDK. Resource filenames were discovered with `jpackage --verbose`, which logs each template it uses — don't guess them.
- macOS icons are expected to occupy ~80% of their canvas with transparent margin (the system implies the badge shape), unlike Linux/Windows full-bleed, so the generator pads the `.icns` sources. `png2icns` (icnsutils 0.8.1) tops out at the 512px `ic09` type and skips 1024px `ic10`.

### About dialog
- **Help → About OAS Buddy** (`MainApp.buildMenuBar`) opens `no.maddin.oasbuddy.desktop.pane.AboutDialog`, split the same way `RemovalConfirmation` is: `content(AppInfo, Consumer<String>)` builds the body and `show(...)` wraps it in a modal `Alert`, so the content is testable without driving a modal window. Everything it displays comes from `AppInfo` (see Versioning above), and an empty field means the line or link is omitted rather than rendered blank.
- Links are handed to a `Consumer<String>` rather than opened directly. The app passes `MainApp.browse`, which wraps `getHostServices().showDocument(url)` in a catch-and-log: `HostServices` only exists in an application JavaFX launched itself (a TestFX test constructs `MainApp` directly), and a desktop with no registered handler fails at `showDocument`. A dead About link is not worth a stack trace.
- The icon comes from `AppIcon.vector(edgeLength)` — live vector, no bitmap. It wraps the scaled master group in a second `Group` because a node's own transforms don't count towards its layout bounds; without the wrapper the icon still measures 256px to its layout parent and blows the dialog open. The PNG round-trip `AppIcon.image(int)` needs is a window-icon concern only and is deliberately not used here.

### Reading a document must never change it
- Optional sections resolve through `core.document.LazyObjectNode`: `peek()` for reads (returns `null` when the section is absent), `create()` for writes (adds it and any missing ancestor). `OasDocument.getComponents()`, `Components.getSchemas()` and `Components.getSecuritySchemes()` all go through it.
- **Why it exists:** those used `JsonNodes.objectChild`, which *creates* the child. The outline asks for every section each time it is rebuilt, so merely opening a file grew a `components: {schemas: {}}` block that was never in it — and saving wrote it out. Noise in exactly the git diff this editor exists to keep clean. `SecuritySchemesTest.readingASectionNeverAddsItToTheDocument` guards it.
- `info` and `paths` deliberately keep the eager `JsonNodes.objectChild`: every valid OAS 3.0 document has both, so there is nothing to avoid creating.

### Schema structure (items, references, additionalProperties)
- A type picker writes through `Schema.changeTypeTo` / `Schema.referTo`, which drop the keys the new choice no longer uses: `referTo` removes `type`, `format` and `items` (and nothing else — a `description` or `x-` extension is not the picker's to drop); `changeTypeTo` removes `$ref`, and `items` unless the type is `array`. `setType` stays a plain setter on purpose: `OperationPane`'s parameter type is a free-text field calling it per keystroke, and typing over `array` must not discard its `items` halfway.
- `getItems()` / `getAdditionalPropertiesSchema()` read only (return `null` when absent); `createItems()` / `createAdditionalPropertiesSchema()` write — the same rule as `LazyObjectNode`. Choosing a type for items is what creates them; an `array` with no `items` is left for validation to report.
- In `SchemaPane` a property's single type picker lists the JSON types followed by `→ Name` for every declared schema (`pane.TypeChoice`), so pointing a property at a schema is one choice. Picking "of type…" for additionalProperties writes an empty schema straight away (equivalent to `true`), so the document never disagrees with the picker while no type is chosen yet.

### Schema values and metadata (enum, default, example, flags)
- **One rule for entering typed values**, in `core.model.SchemaValues`: text is read as JSON when that yields a value of the schema's type (`42` for `integer`, `true` for `boolean`, `{"a":1}` for `object`); a `string` schema takes the text literally; anything that does not fit falls back to a literal string rather than being refused, so validation reports it — the "editing an invalid spec is always allowed" principle. The type is read at the moment of entry; changing the type later does not re-parse existing values.
- `nullable`, `readOnly`, `writeOnly`, `deprecated` all default to false, so `Schema.setFlag` writes only `true` and removes the key otherwise. An empty `enum` removes the key rather than leaving `[]`.
- `pane.SchemaDetails` is the one editor for these fields, used for the schema itself (its "Values" section) and inside each property's collapsible Details row. Its controls carry style classes, not ids, because several can be on screen at once and a property name must never go into an id. A reference property gets no Details: OAS 3.0 ignores everything beside a `$ref`.

### Schema validation constraints
- `core.model.Constraint` is the single statement of which keyword belongs to which type and what value it takes (number, non-negative count, boolean, text); the editor (`pane.SchemaConstraints`) and the validator both read it. `exclusiveMinimum`/`exclusiveMaximum` are **booleans** in OAS 3.0 (JSON Schema draft 4), not the numeric form of later drafts.
- **Changing a schema's type hides constraints, it does not delete them** — deliberately, so an `integer` that briefly becomes a `number` or `string` and back keeps its bounds. To avoid invisible state, the section shows a note naming every constraint in the file that the current type does not use (`Schema.constraintsNotApplying()`).
- Numbers are written as JSON numbers. The editor accepts *any* number, even a negative or fractional count, and leaves it for validation to report; only text that is no number at all is refused — the field turns red and the last valid value stays in the document.
- **swagger-parser does not check constraints** (verified: it accepts `minLength` > `maxLength`, a negative `minItems`, a string `minimum`, a numeric `exclusiveMinimum`; it only flags `multipleOf <= 0`). `core.validation.ConstraintCheck` fills that gap and `OasValidator` merges its messages. It finds schemas by where OAS puts them (component schemas, any `schema` key, and within a schema `properties`/`items`/`additionalProperties`/`allOf`/`anyOf`/`oneOf`/`not`), skipping `example`/`default`/`enum` data, so a property *named* `minLength` is not mistaken for a constraint.
- These are reported as errors, and `MainApp.runValidation` shows both severities (see Tags below for where a warning comes from and how the panel renders it).

### Security schemes
- `core.model.SecurityScheme` covers four of the five types OAS 3.0 defines: the three whose shape is flat strings — `apiKey`, `http`, `openIdConnect` — and `oauth2`.
- **oauth2** is editable too: `SecurityScheme.getFlows()` reaches the nested `flows` object (lazily, so reading never adds an empty one), and `pane/OAuthFlowsPane` is its editor — the four flows switched on and off independently, each with its own scope map. Which URLs a flow carries lives in `OAuthFlows.urlsOf(...)`, not in the pane, so the spec is stated once: `implicit` has no token URL, `clientCredentials` has no authorization URL.
- `OAuthFlow.renameScope` rebuilds the scope map rather than remove-then-add, because Jackson cannot rename a field and remove-then-add would push the scope to the end — every rename would reorder the saved file, which is exactly what the ordered tree exists to prevent. For the same reason a rename commits on Enter or focus-loss, not per keystroke: rebuilding the map on every character would fight the caret.
- Two edits discard nested data, and both ask first *only when there is something to lose* — an empty flow or an empty `flows` goes silently, since a dialog about nothing teaches the user to dismiss dialogs: turning a populated flow off, and changing a populated oauth2 scheme to another type. Declining leaves the document **and the control** as they were; both listeners guard against re-entry while reverting, or the revert would fire the listener again.
- The "cannot be edited yet" branch in `SecuritySchemePane` still exists, now for types with no form at all (`mutualTLS`, or a typo). Such a scheme gets no type picker, so it cannot be converted by accident, but can still be deleted.
- `setType` strips the fields of the *other* editable types. The strip list is the union of those three types' own fields and nothing else, so `flows` and any `x-` extension survive whatever happens around them — the editor must never quietly drop what it cannot edit. Leaving the stale fields instead was rejected: a leftover `name` on an `http` scheme is invisible in the form (that row is not rendered for http) yet still gets written to the file, and silent state that changes the output is worse than losing a value the user can retype.

### Applying security (requirements)
- `security` is an array of **alternatives** — satisfying any one entry is enough — and the schemes named *inside* one entry are required together. The editor only builds single-scheme entries; a combined one loaded from a file is shown with its schemes named, no editing controls and a Remove button, the same bargain as a scheme type with no form. Hiding it was rejected: the user would be looking at a security list that disagrees with their file.
- **Three states, all of which must stay expressible:** no `security` key (an operation inherits the document default), an empty array (explicitly public, overriding the default with nothing), and a non-empty array (override). `SecurityRequirements` therefore never creates the key on a read — reading an inheriting operation must not quietly turn it public — which is why it takes its parent node and field name rather than the array.
- At **document** level an absent key and an empty array mean the same thing, so `SecurityPane` removes the key once the last requirement goes, keeping the file clean. At **operation** level they differ, so `OperationPane` keeps them apart with a three-way radio. That radio's selection is read from the document on build but held as pane state afterwards: choosing "Require" declares an empty array, which on its own reads back as "public", and the radio must not jump away while the user is about to add the first requirement.
- `SecurityScheme.declaredScopes()` is the union of the oauth2 flows' scopes, because a requirement names the *scheme*, not one of its flows, so a scope any flow defines is legitimate to require.
- The finder the requirements need already existed before this: `core.model.SecuritySchemeUsages` (added with the removal scan), which is what the confirmation dialog lists when a still-required scheme is removed.
- **Gotcha: never build a JavaFX node id out of user data.** OAuth scope names routinely contain a colon (`read:pets`), and TestFX's `lookup("#...")` is a CSS *id selector* where `:` begins a pseudo-class — so the lookup parses as something else and silently matches nothing. The scope checkboxes carry no id at all; their own label identifies them, and the tests find them that way.

### Tags
- `core.model.Tags` is a lazy read over the root `tags` array, the same rule `SecurityRequirements` follows at document level: absent and empty mean the same thing (a document defining no tags), so `names()`/`get(name)` never create the key, and `remove(name)` takes the key out again once the last tag is gone. `Tag` covers only `name` (read-only — no `setName`; renaming would need to walk every operation that names it, out of scope for now) and `description`; `externalDocs` and any `x-` extension on a tag are left untouched.
- **An operation naming a tag the root list never declares is not an error.** OAS does not require it, unlike a duplicate root tag name, which really is a spec violation. So `core.validation.TagCheck` reports an undefined tag at `WARNING`, not `ERROR` — checked by `TagCheckTest.undefinedTagsAreReportedAsWarningsRatherThanErrors`, which also asserts the document still reads as valid. A loaded file using one is not silently rewritten (no auto-declaring on open) — that would be the same mistake `LazyObjectNode` exists to prevent, just at the root array instead of a section.
- **Duplicate tag names are deliberately not re-checked in `TagCheck`.** swagger-parser already reports `attribute tags.<name> is repeated` for a duplicate root name (verified for both YAML and JSON) at `ERROR`, so checking it again would just show the same problem under a second message.
- `MainApp.runValidation` now shows both severities, since a warning would otherwise never surface: each message is prefixed (`"Warning: "` / `"Error: "`) so they read apart at a glance, and the "No validation errors." fallback appears only when the message list is empty — which is exactly "neither errors nor warnings", so the text stays accurate now that a warning-only document also produces messages.
- `core.model.TagUsages` is the "which operations use this" finder for tags, the counterpart to `SchemaReferences` / `SecuritySchemeUsages`. It renders each usage as `GET /pets` rather than an arrow path (`paths → /pets → get → tags → [0]`): a tag can only ever appear in an operation's own `tags` array, so there is exactly one shape a usage can take, and the friendlier form loses nothing. `pane.TagRemoval` lists these usages in the confirmation and, like a dangling `$ref`, leaves them for the validation panel to report rather than rewriting the operations.
- The desktop outline gets one flat "Tags" node (after Servers) listing name + description with add/remove, the same shape as `ServersPane` — no per-tag child nodes, since a tag has nothing nested worth its own pane.
- **The operation tag picker replaces the old free-text "Tags (comma separated)" field** with one checkbox per defined tag, plus one per tag the operation already uses that is *not* defined — shown checked and suffixed `(undefined)` with a `Styles.WARNING` style class, rather than hidden, so the form never disagrees with a loaded file. No checkbox carries an id built from the tag name: the same gotcha that rules out an id from an OAuth scope name applies here, so tests find a box by its own label text. A text field + "Add tag" button (Enter too, via the field's own `onAction`) declares a typed name in the root list and adds it to the operation in the same action, so tagging a brand-new tag stays one step instead of two. Toggling a checkbox rebuilds the whole picker rather than patching it in place — unlike the security-radio pattern this mirrors, a checkbox has no caret to protect, and rebuilding is what makes an unticked `(undefined)` row disappear again. `Operation.setTags` removes the `tags` key on an empty list rather than leaving `[]`, matching every other collection in this codebase.

### Removing things from the document
- Every removal (schema, path, operation) goes through a confirmation: `no.maddin.oasbuddy.desktop.pane.RemovalConfirmation` is a one-method interface (`confirm(question, details)`) with a `dialog()` implementation for the running app, so the removal flows are testable without driving a modal `Alert`. `SchemaRemoval` and `PathRemoval` compose the `details` text and apply the change; the panes only *report* a removal request via a callback and never mutate the document themselves.
- Removing a security scheme scans for the `security` requirements that name it (`core.model.SecuritySchemeUsages`) and lists them in the dialog, left dangling for validation to report — the same bargain as schema `$ref`s, but schemes are referenced by bare name, so the scan keys on the enclosing `security` array rather than on the name alone (a schema property that happens to share the name is not a usage).
- Removing a schema first scans the whole raw tree for `$ref`s to it (`core.model.SchemaReferences`) and lists them in the dialog; the refs are left dangling for the validation panel to report rather than being rewritten. Removing a path lists the operations that go with it. Nothing can `$ref` a path or an operation, so those need no scan.
- After any removal `MainApp.refreshAndSelect(labels...)` rebuilds the outline and selects a surviving node by label, so the editor is never left showing something that was just deleted.

### Logging
- The app's own logging uses `System.getLogger` (the JDK platform logger), which lands in `java.util.logging`. No logging dependency for our own code.
- swagger-parser logs through **slf4j**, whose API arrives transitively via `oas-buddy-core`. The **provider is declared in `oas-buddy-desktop`, not in core**: picking one is the application's job, and a library that picks a provider forces it on every consumer. `oas-buddy-core` therefore ships the API only, and its own test runs still print slf4j's "No providers were found" banner — that is correct for a library, not a bug to chase.
- The provider is `org.slf4j:slf4j-jdk14` (scope `runtime`), which routes slf4j **into** `java.util.logging`, so library and application diagnostics end up in one place. Without it slf4j falls back to a no-op logger and silently discards everything swagger-parser has to say about a document it could not parse — the banner on stderr is the cosmetic half of that problem, the lost diagnostics are the real half.
- **Don't confuse it with `org.slf4j:slf4j-jdk-platform-logging`**, which is a `System.LoggerFinder` routing the *other* direction (`System.Logger` → slf4j). It is not an slf4j provider, would not silence the banner, and combined with a NOP fallback would send our own logging nowhere.
- `slf4j-bom` is imported in the root POM's `dependencyManagement` so the API version swagger-parser drags in cannot drift from the provider's.
- `LoggingProviderTest` asserts a provider is present *and* that both an slf4j call and a `System.getLogger` call arrive in `java.util.logging`, so the routing is checked rather than just the dependency.

### Error handling principles
- Malformed YAML/JSON on open: show error with line/column, never crash.
- Spec validation errors/warnings: shown in a non-blocking panel; editing and saving an invalid/WIP spec is always allowed.
- File I/O errors: dialog with retry.

### Roadmap beyond MVP (directional, not committed)
1. OAS 3.1 support (extend typed facade)
2. Swagger 2.0 support
3. YAML comment preservation on round-trip
4. Server backend + real-time collaboration with colleagues
5. IntelliJ plugin front-end reusing oas-buddy-core
6. External $ref resolution, raw-text/source split view

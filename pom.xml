<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>no.maddin.oasbuddy</groupId>
  <artifactId>oas-buddy</artifactId>
  <version>${revision}</version>
  <packaging>pom</packaging>

  <name>OAS Buddy</name>
  <description>An OpenAPI Specification editor.</description>
  <url>${repository.url}</url>
  <inceptionYear>2026</inceptionYear>

  <!-- Together with inceptionYear this is where the About dialog's copyright line comes from. -->
  <organization>
    <name>OAS Buddy contributors</name>
    <url>${repository.url}</url>
  </organization>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
    </license>
  </licenses>

  <!-- Credited by name in the About dialog, which also links to the full contributor graph. -->
  <developers>
    <developer>
      <id>maddingo</id>
      <name>Martin Goldhahn</name>
      <url>https://github.com/maddingo</url>
    </developer>
  </developers>

  <modules>
    <module>oas-buddy-core</module>
    <module>oas-buddy-desktop</module>
  </modules>

  <properties>
    <!-- The repository, as a property rather than read back from ${project.url}: Maven appends the
         module's artifactId to an inherited url, so ${project.url} in a child module points at a
         path that does not exist. The desktop module filters this into version.properties to build
         the About dialog's links. Verified with Maven 3.9.16, where the documented
         child.project.url.inherit.append.path="false" attribute does not suppress the append. -->
    <repository.url>https://github.com/maddingo/oas-buddy</repository.url>

    <!-- CI-friendly version: overridden with -Drevision=X.Y.Z when building a release; the
         default here is what every ordinary local/CI build (test, package) uses. -->
    <revision>1.0.0-SNAPSHOT</revision>

    <maven.compiler.source>25</maven.compiler.source>
    <maven.compiler.release>25</maven.compiler.release>
    <maven.compiler.target>25</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <jackson.version>2.19.0</jackson.version>
    <junit.version>5.12.2</junit.version>
    <swagger-parser.version>2.1.27</swagger-parser.version>
    <slf4j.version>2.0.9</slf4j.version>
    <javafx.version>25.0.4</javafx.version>
    <testfx.version>4.0.18</testfx.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Keeps slf4j-api, which arrives transitively via swagger-parser, pinned to the same
           version as the provider the desktop module installs. -->
      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-bom</artifactId>
        <version>${slf4j.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.swagger.parser.v3</groupId>
        <artifactId>swagger-parser</artifactId>
        <version>${swagger-parser.version}</version>
      </dependency>
      <dependency>
        <groupId>no.maddin.oasbuddy</groupId>
        <artifactId>oas-buddy-core</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- Without this, a standalone `mvn install` writes the literal, unresolved "${revision}"
           text into the installed POM (it only resolves within a live reactor build), which then
           breaks any later separate build - e.g. `cd oas-buddy-desktop && mvn javafx:run` - that
           tries to resolve oas-buddy-core as a dependency from the local repo. This rewrites the
           installed/deployed POM with the real version baked in, leaving pom.xml itself alone. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <version>1.8.0</version>
        <configuration>
          <flattenMode>resolveCiFriendliesOnly</flattenMode>
          <updatePomFile>true</updatePomFile>
        </configuration>
        <executions>
          <execution>
            <id>flatten</id>
            <phase>process-resources</phase>
            <goals>
              <goal>flatten</goal>
            </goals>
          </execution>
          <execution>
            <id>flatten-clean</id>
            <phase>clean</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
